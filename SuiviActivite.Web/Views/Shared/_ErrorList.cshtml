<ul>
    @foreach (var errorInput in ViewData.ModelState.Where(m => m.Value.Errors.Count > 0))
    {
        foreach (var error in errorInput.Value.Errors)
        {
            <li class="field-validation-error">@error.ErrorMessage</li>
        }
    }
</ul>